{
  "name": "reshape-code-gen",
  "description": "converts a reshape ast into a javascript function",
  "version": "0.2.1",
  "author": "Jeff Escalante",
  "ava": {
    "verbose": "true"
  },
  "bugs": "https://github.com/reshape/code-gen/issues",
  "dependencies": {
    "objectfn": "^2.0.0",
    "with": "^5.1.1"
  },
  "devDependencies": {
    "ava": "^0.17.0",
    "coveralls": "^2.11.12",
    "nyc": "^10.0.0",
    "reshape": "reshape/reshape",
    "reshape-custom-elements": "reshape/custom-elements",
    "reshape-parser": "^0.2.0",
    "standard": "8.6.0"
  },
  "engines": {
    "node": ">= 6.0.0"
  },
  "homepage": "https://github.com/reshape/code-gen",
  "keywords": [
    "code-generator",
    "reshape"
  ],
  "license": "MIT",
  "main": "lib",
  "repository": "reshape/code-gen",
  "scripts": {
    "coverage": "nyc ava",
    "coveralls": "nyc --reporter=lcov ava && cat ./coverage/lcov.info | coveralls",
    "lint": "standard",
    "test": "ava"
  }
}
